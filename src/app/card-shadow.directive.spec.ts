import { CardShadowDirective } from './card-shadow.directive';

describe('CardShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new CardShadowDirective();
    expect(directive).toBeTruthy();
  });
});
